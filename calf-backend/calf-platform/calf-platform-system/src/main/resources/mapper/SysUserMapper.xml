<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qinweizhao.system.mapper.SysUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.qinweizhao.system.entity.SysUser">
        <id column="user_id" property="userId"/>
        <result column="dept_id" property="deptId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nick_name" property="nickName"/>
        <result column="sex" property="sex"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="avatar" property="avatar"/>
        <result column="user_sort" property="userSort"/>
        <result column="deleted" property="deleted"/>
        <result column="enabled" property="enabled"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectUserByUsername" resultType="com.qinweizhao.system.entity.SysUser">
        SELECT *
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="selectRolesByUserId" resultType="java.lang.String">
        SELECT r.role_key
        FROM sys_role r
                 LEFT JOIN sys_user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{id}
    </select>

    <select id="selectPermissionsByUserId" resultType="java.lang.String">
        SELECT permission
        FROM sys_menu m
                 LEFT JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
                 LEFT JOIN sys_role r ON rm.role_id = r.role_id
                 LEFT JOIN sys_user_role ur ON ur.role_id = r.role_id
        WHERE ur.user_id = #{userId}
    </select>


</mapper>
